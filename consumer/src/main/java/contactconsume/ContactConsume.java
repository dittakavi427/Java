package contactconsume;

import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Service;
import org.springframework.web.client.RestTemplate;

import com.contact.consumer.model.Contact;

@Service
public class ContactConsume {

	
	/*
	 * public static String base_url="http://localhost:8080/contact/create";
	 * 
	 * public static void main(String[] args) { RestTemplate restTemplate = new
	 * RestTemplate(); Contact contact=new Contact("lastname1", "firstname1",
	 * "emailer1@der.com", 1245515416L);
	 * 
	 * ResponseEntity<Contact> re=restTemplate.postForEntity(base_url,
	 * contact,Contact.class); System.out.println(re.getStatusCodeValue()); }
	 */

}
